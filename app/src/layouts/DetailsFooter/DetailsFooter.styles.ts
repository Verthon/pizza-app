import styled from "styled-components"

const Footer = styled.footer`
  display: flex;
  flex-direction: column;
  width: calc(100% - 4rem);
  position: fixed;
  bottom: 2rem;
`

export const Styled = {
  Footer,
}